# Defer Departures in the MVP

TripOS will model a **Trip** as one scheduled sellable offering in the MVP and will introduce **Departure** later when organizers need multiple date ranges under the same reusable trip. This keeps the first operational model simpler across capacity, pricing, payment schedules, and bookings, while accepting temporary duplication when the same trip runs on multiple dates.
